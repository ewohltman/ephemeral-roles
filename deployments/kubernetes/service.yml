apiVersion: v1
kind: Service
metadata:
  name: ephemeral-roles
  namespace: ephemeral-roles
spec:
  selector:
    app: ephemeral-roles
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer